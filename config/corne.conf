# Turn on RGB Underglow support
CONFIG_ZMK_RGB_UNDERGLOW=y

# Use the LED strip driver required for Corne LEDs
CONFIG_WS2812_STRIP=y

# Optional: Turn off RGB when the keyboard goes to sleep to save battery
CONFIG_ZMK_RGB_UNDERGLOW_AUTO_OFF_IDLE=y
